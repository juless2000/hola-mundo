'use strict'

// BUCLE FOR
// Bucle es una estructura de control que se repite varias veces
// El primer elemento es un contador
// El segundo elemento es una codicion
// El tercero, incrementador, o decrementador del contador

var numero = 100;

for(var i = 0; i <= numero; i++){
	console.log("Vamos por el número: "+ i);

	//debugger; //Podemos ver lo que pasa en el bucle
}