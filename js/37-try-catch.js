'use strict'

// Capturar el error en un codigo
// Se puede mandar un mensaje para el error
try{
	var year = 2022;

	alert(year);
	// Sirve para las URL decodificadas 
	//console.log(decodeURIComponent("https://y%%u##$$tube.com"));

	var vector = new Array(99999999999999999);

}catch(error){
	console.log(error);
	alert("Ha ocurrido un error en el codigo");
}
